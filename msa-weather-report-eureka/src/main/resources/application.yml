server:
  port: 8088

spring:
  # 热部署静态文件
  thymeleaf:
    cache: false
  application:
    name: msa-weather-report-eureka

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka